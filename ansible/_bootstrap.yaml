---

  - hosts: bastion node control-plane
    # linear means do not use mitogen, which requires python
    strategy: linear
    any_errors_fatal: true
    name: "Bootstrap Nodes"
    gather_facts: no
    become: yes
    vars:
      gather_reachability_data: false

    roles:
      - wait-ssh
      - bootstrap-python
