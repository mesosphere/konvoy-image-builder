---
- name: create offline directory
  file:
    path: "{{ offline.os_packages_remote_bundle_path }}"
    state: directory

- name: upload and extract offline bundle
  unarchive:
    copy: true
    src: "{{ offline.os_packages_local_bundle_path }}"
    dest: "{{ offline.os_packages_remote_bundle_path }}"
